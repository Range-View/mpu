cmake_minimum_required(VERSION 3.14)

# Set the project name and version
project(MPU VERSION 1.0)

# Specify C++ standard
set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED True)

# Optionally set up any flags or initial settings
# set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall")

# Add subdirectories for each module
add_subdirectory(MPUMainApp)
add_subdirectory(IO)
add_subdirectory(UI)
add_subdirectory(Analysis)

# If you have a libs folder with CMakeLists inside them, uncomment the following line
# add_subdirectory(libs)

# Configure any global settings, if needed
# For example, to copy or configure a global resource file
# configure_file("SomeGlobalConfig.h.in" "SomeGlobalConfig.h")

# If there are inter-project dependencies, they can be set here
# For example:
# add_dependencies(UI MPUMainApp)
